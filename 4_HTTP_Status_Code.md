1) What are the http status codes ?
2) what happens at server side ?
2) what happens at client side ?
4) All types of status codes and meaning of them starting with 2, 3, 4, 5 etc
5) Most commonly used status code ?
6) WHat is HTTPException and how can we use it for sending the status code as response or custom error